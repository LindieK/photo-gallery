import React from 'react'
import {CollectionWrapper, Placeholder,} from '../../styles/ProfileStyles'
import LazyLoad from 'react-lazyload'

export default function ImageCollection() {
    return (
        <CollectionWrapper>
            <LazyLoad>

            </LazyLoad>
        </CollectionWrapper>
    )
}
